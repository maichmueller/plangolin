
(define (problem blocks-2-0)
    (:domain blocks)

    (:objects a b )

    (:init (clear a)
           (clear b)
           (ontable a)
           (ontable b)
           (handempty))

    (:goal (on a b))
    )
