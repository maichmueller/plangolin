seed_everything: true

data:
  batch_size: 26
  parallel: False
  balance_by_distance_to_goal: True

encoder:
  class_path: HeteroGraphEncoder

value_estimator:
  gamma: 0.9

optimizer_setup:
  optimizer:
    class_path: torch.optim.Adam
    init_args:
      lr: 0.002
  lr_actor: 0.0002

data_layout:
  input_data:
    domain_name: blocks
    instances: [ small, medium ]
    validation_instances: [ small ]
  root_dir: null

trainer:
  accelerator: auto
  devices: 1
  max_epochs: 1
  logger:
    class_path: lightning.pytorch.loggers.WandbLogger
    init_args:
      project: rgnet
      group: rl-test
      offline: True

model:
  gnn:
    class_path: HeteroGNN
    init_args:
      num_layer: 8
      hidden_size: 3
      aggr: logsumexp
